# 1920x440 Ultrawide UI Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Implement the first production-ready 1920x440 ultrawide adaptation with bounded shell metrics and a minimalist Job Status screen.

**Architecture:** Reuse the existing `metrics.ultraWide` hook and avoid changing command behavior. Add shared metrics for touch sizes and bounded shell dimensions, then update QML panels to consume those metrics. Start with Job Status because it has the clearest design change and the highest visual risk.

**Tech Stack:** PySide6/QML, pytest QML structure tests, existing screenshot capture tool.

---

### Task 1: Ultrawide Metrics And Shell Bounds

**Files:**
- Modify: `src/klippertouch/qml/Metrics.qml`
- Modify: `src/klippertouch/qml/components/ActionBar.qml`
- Test: `tests/qml/test_qml_loads.py`

- [x] **Step 1: Write failing tests**

Add tests that require bounded `ultraWideActionBarWidth`, reusable touch metrics, and `ActionBar` to use `minimumTouchSize` for spacing and cells.

- [x] **Step 2: Run tests and verify failure**

Run: `UV_INDEX_URL=https://pypi.org/simple uv run --locked pytest tests/qml/test_qml_loads.py -q`

- [x] **Step 3: Implement metrics and shell bounds**

Add `minimumTouchSize`, `safeTouchSize`, `compactRowHeight`, `panelColumnGap`, and `ultraWideActionBarWidth`. Use the bounded action bar width in landscape ultrawide mode.

- [x] **Step 4: Run QML tests**

Run the same QML test file and expect pass.

### Task 2: Minimalist Ultrawide Job Status

**Files:**
- Modify: `src/klippertouch/qml/panels/JobStatusPanel.qml`
- Test: `tests/qml/test_qml_loads.py`

- [x] **Step 1: Write failing tests**

Add tests that require a `ultraWideMinimalSummaryGrid`, exactly three primary metric cards for layer, remaining time, and elapsed time, plus an icon-only player control cluster.

- [x] **Step 2: Run tests and verify failure**

Run: `UV_INDEX_URL=https://pypi.org/simple uv run --locked pytest tests/qml/test_qml_loads.py -q`

- [x] **Step 3: Implement minimalist summary**

Replace the existing information-dense ultrawide summary with a spacious composition: filename/status, layer card, remaining card, elapsed card, and icon-only controls. Keep detail pages for advanced data.

- [x] **Step 4: Run QML tests**

Run the QML test file and expect pass.

### Task 3: Screenshot Validation

**Files:**
- Read: `tools/capture_qml_screenshots.py`
- Output: `artifacts/qml-screenshots/`

- [x] **Step 1: Capture screenshots**

Run: `QT_QPA_PLATFORM=offscreen UV_INDEX_URL=https://pypi.org/simple uv run --locked python tools/capture_qml_screenshots.py --sizes 1920x440,800x480,1024x600,480x800 --panels job_status`

- [x] **Step 2: Inspect 1920x440 output**

Verify no overlap, no dense metric table, and touch buttons remain large.

### Task 4: Final Verification

**Files:**
- All modified files

- [x] **Step 1: Run focused tests**

Run: `UV_INDEX_URL=https://pypi.org/simple uv run --locked pytest tests/qml/test_qml_loads.py -q`

- [x] **Step 2: Run diff check**

Run: `git diff --check`

- [x] **Step 3: Review diff**

Run: `git diff -- src/klippertouch/qml/Metrics.qml src/klippertouch/qml/components/ActionBar.qml src/klippertouch/qml/panels/JobStatusPanel.qml tests/qml/test_qml_loads.py`
